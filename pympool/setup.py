from skbuild import setup  # This line replaces 'from setuptools import setup'
setup(
    name="py_mpool",
    version="1.0.0",
    description="py ext",
    packages=['pympool'],
    python_requires=">=3.7",
)